import { Helmet } from "react-helmet-async";

const UserProfile = () => {
    return (
        <div>
            <Helmet>
                <title>Cozy Reside | Profile</title>
            </Helmet>
            <h1>user profile</h1>
        </div>
    );
};

export default UserProfile;